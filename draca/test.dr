(define/in-namespace std::math-fns::fact (define (fact n) (if (<= n 1) 1 (* n (fact (- n 1))))))

(define x 50)

(println "{0}" x)

(define (rev lst)
  (if (empty? lst)
      (list)
      (append (rev (cdr lst))
              (list (car lst)))))

(println "{0}" (rev '(1 2 3 4)))
