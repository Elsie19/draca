# TODO

Make evaluation print at the end of the last parens instead of newline, so:

```
\> (+ 1
    (*
        6
        7
    )
) => 43
\>
```

Instead of:

```
\> (+ 1
    (*
        6
        7
    )
)
43
\>
```
