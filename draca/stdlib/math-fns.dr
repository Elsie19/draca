(define/in-namespace std::math::fns::abs
    (define (abs x)
        (if (< x 0)
            (- x)
            x)))

(define/in-namespace std::math::fns::square
    (define (square x)
        (* x x)))

(require std::math::fns)
